# My Dotfiles

## Setup

```
$ stow fish/ -t ~/.config/fish/
$ stow git/ -t ~
$ stow ssh/ -t ~/.ssh/
$ stow tmux/ -t ~
$ stow vim/ -t ~
# Options
$ stow fish-lambda-theme -t ~/.local/share/omf/themes/lambda/functions/
```

Add `--adopt` flag if you already have your config and use with caution.
